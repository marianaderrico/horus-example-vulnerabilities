name: Pipeline

on: [push]

jobs:
  security:
    name: security
    runs-on: ubuntu-latest
    container: horuszup/horus-cli
    steps:
      - name: Running Horus
        run: /bin/horus start -p="./"